# 다음 작업 — 화이트보드 프로그램 입력

**브랜치**: `mmyeon/program-input` (워크트리 `~/orca/workspaces/YeokdoIn/program-input`)
**작성**: 2026-09-11
**상태**: tasks.md 43개 중 42개 완료. 그 뒤 실사용 피드백으로 추가 작업이 쌓였다.

품질 게이트는 현재 전부 통과한다 — `npm run test` 232개, `type-check`, `build`.
`features/programs/model` 커버리지 98.0% / 95.8%(branch).

---

## 0. 지금 어긋나 있는 것

### ~~N1. 문서-코드 불일치 — 「괄호 밖의 `+`」 검출 규칙~~ — 2026-09-11 해소

`suspect-spans.ts` 에 `unbracketed-plus` 규칙을 구현했다. SC-005 기대값은 3건 → 4건.

이때 내린 판정:

- **표시 구간은 덩어리 전체**(`2+1`). `+` 한 글자가 아니다 — 고칠 것이 `2+1` → `(2+1)` 이므로
- **「괄호 안」은 짝이 맞는 쌍 안**을 뜻한다. `(2+1×4`, `2+1)×4` 처럼 한쪽만 남은 것은
  그 자체가 유실 신호이므로 검출한다
- 둘 다 문서 4.2 에 적었다

---

## 1. 오인식 검출 확대 (문서 먼저)

2026-09-11 실제 아이폰 사진앱 인식 결과를 넣어보니 **6줄 전체에서 검출 0건**이었다.

```text
Militery Press 5x3
Power Snatch & Squat Snatch 65% (1+1)x1,20%(2+1)x2, 25% (1+1)x 2, Squat snatch Bor. 2xd, Bfr 1xz.
Powet clean & Squat dean
65% (1+1)x2, %x. (211)x2, 25% (1+1) x2.
Squnt clean Box.2xs, Bsx 1xs.
Bade squat 7o%.
```

### N2. 의심 문자 후보를 `O l I` → 임의 문자로 확대

현재 규칙이 후보를 세 글자로 한정해 `7o%`(소문자 o), `2xd`, `1xz`, `2xs`, `1xs` 를 전부 놓친다.
문서 4장의 원칙(「숫자 자리에 문자가 오면 오인식」)은 그대로 맞고, **후보를 좁힌 것이 틀렸다.**

- `x`/`×` 뒤와 `%` 앞은 숫자만 오므로 **아무 문자나** 의심해도 오탐이 없다
- 문서 4.2 를 먼저 고치고 `suspect-spans.ts` 를 따라가게 한다
- SC-005 골든 케이스로 오탐 0건을 재확인할 것

**2026-09-14 추가 표본** — 두 번째 아이폰 인식 결과 13줄에서도 **검출 0건**이었다.

```text
Hinh Hana Pomar Suta SALC
65%(3+1)X2 70%(3+1)X2
Hang Snatch 75% 3×3
Hann
Sant Subl
80%2x| 85%2x1 90%Ix1 93~95%1x1
High Hang Power Clean & F. Squat 65%(3+)x2 70/(3+DX):
Hang Clean
15%2x3
Hang Squat Clean
80/241 942
4 90%1
M 93:95/×
```

놓친 것과 이유:

| 놓친 것 | 왜 |
| --- | --- |
| `80%2x\|` — `\|` ← `1` | 후보가 `O l I` 뿐 (N2 본문) |
| `90%Ix1` — `I` ← `1` | **새 구멍.** `%` 바로 **뒤** 렙 자리. 현재 규칙은 `%` 앞만 본다 |
| `70/(3+DX):` — `/` ← `%`, `D` ← `1` | N3 미구현 + 후보 한정 |
| `80/241 942`, `M 93:95/×` — `/` ← `%`, `:` ← `~` | N3 미구현. `:` ← `~` 는 미기록 패턴 |
| `(3+)x2` — `+` 뒤 숫자 유실 | 괄호 안이라 정상 판정. 판정 근거 없음 |
| `15%2x3` — `1` ← `7` | 유효한 숫자라 원리적으로 탐지 불가 (N4) |

`90%Ix1` 이 핵심이다. 「`%` 뒤 숫자 자리」도 숫자만 오는 자리이므로 오탐 없이 잡을 수 있는데
현재 규칙에 그 위치가 없다. N2 를 할 때 `%` 앞뒤를 함께 넓혀야 한다.

### N3. `/` ← `%` 패턴 기록 + 검출

`Front Squat 15/3x3,80/3x3` 처럼 `%` 가 `/` 로 자주 읽힌다. 6판 원문에 `/` 가 한 번도
나오지 않으므로 「숫자 뒤 `/`」는 오탐 위험이 낮다.

- `docs/gym-program-notation.md` 4.1 에 사례, 4.2 에 규칙 추가 → 그다음 코드

### N4. 못 잡는 것 (기록만, 구현 대상 아님)

- `Bor.`, `Bfr`, `Box.`, `Bsx` ← `80%`/`85%` — 숫자가 아예 없어 판정 근거가 없다
- `20%`←`70%`, `25%`←`75%` — 결과가 유효한 숫자라 **원리적으로 탐지 불가**
- `Militery`, `Powet`, `dean`, `Squnt`, `Bade` — 종목명 오타. 어휘 대조가 있어야 가능하다
- `(211)` ← `(2+1)` — `+` 가 `1` 로 읽힘. 숫자만 남아 정상으로 보인다
- 줄바꿈 유실 (3·4번 줄이 원래 한 줄)

---

## 2. 편집 부담 줄이기 (새 FR 필요 — 별도 이슈)

현재 스펙에 대응 FR 이 없다. spec.md 에 FR 을 먼저 추가해야 한다.

### N5. 원탭 치환 칩 (우선순위 높음)

의심 구간을 탭하면 후보 문자가 칩으로 뜨고 한 번 더 탭하면 교체.
`l→1`, `I→1`, `O→0`, `o→0`, `/→%`, `.→,`, `z→2`, `s→5`.

지금은 4동작(항목 탭 → 캐럿 이동 → 백스페이스 → 입력)이 2탭이 된다.
모바일에서 캐럿 옮기는 비용이 가장 크다. **자동 수정이 아니라 사용자가 고르는 것이므로
FR-014(자동 수정 금지)와 충돌하지 않는다.**

### N6. 같은 패턴 일괄 치환

오인식은 사진 한 장 안에서 체계적으로 반복된다. `/` 가 12군데면 12번 고쳐야 한다.
「`/` 12건을 전부 `%` 로」 버튼 하나. `/` 처럼 규칙적인 것에서 효과가 크다.

### N7. 보조 툴바 (우선순위 낮음)

편집 중 텍스트 필드 위에 `0-9 % x ( ) + , ~` 한 줄. 캐럿 자리에 삽입.
`inputMode="numeric"` 은 **쓸 수 없다** — 한 항목에 영문 종목명이 섞여 있다.

### N8. 「다음 의심 구간으로」 순회

20줄에서 3건을 눈으로 찾는 비용이 고치는 비용보다 크다.

---

## 3. 남은 검증

### N9. T041 — quickstart 수동 검증 1~7절

tasks.md 에서 유일하게 안 끝난 항목.

**2026-09-14 결정: 로컬 검증을 건너뛰고 배포 후 실기기에서 수행한다.**
이 기능은 폰 OS 인식 → 복사 → 붙여넣기가 전제라 데스크톱 검증으로는 실사용 환경을
확인하지 못한다. 특히 편집 비용(캐럿 배치)은 실기기에서만 잰다.
따라서 **N10(원격 DB 반영)과 배포가 T041 보다 먼저다.**

로컬에서 하려면: dev 서버 `PORT=3002 npm run dev`, Docker + `npx supabase start` 가 먼저.
폰에서 로컬 서버를 열려면 환경변수의 Supabase URL 이 `127.0.0.1` 이면 안 된다 — 미확인.

자동 검증은 이미 끝냈다: 40줄 분해+검출 0ms(SC-002·SC-003), DB 제약 4종,
`updated_at` 트리거, 갱신 시 `source_text` 불변.

### ~~N10. 원격 DB 반영~~ — 2026-09-15 완료

`20260908120000_programs_text_lines` 를 Studio 에서 직접 실행해 원격 DB 에 적용했고,
`supabase_migrations.schema_migrations` 에도 기록했다. `lines` / `source_text` / `updated_at`
확인 완료. (`npx supabase db push` 대신 수동 실행)

---

## 4. 보류 중인 판단 (사용자 결정 대기)

### ~~N11. OS OCR 전제의 재검토~~ — 2026-09-14 종료

**앱이 직접 OCR 하는 방향은 고려하지 않는다(사용자 결정).**
2026-07-24 결정(「OS 가 OCR 을 대신하므로 앱은 OCR 을 짓지 않는다」)을 유지한다.
인식 품질이 낮다는 사실은 바뀌지 않았으나, 대응은 OCR 교체가 아니라
**검출(N2·N3)과 편집 부담 축소(N5~N8)** 로 한다.

### N12. 정정 사전 (지금은 만들지 말 것)

`source_text`(편집 이전 원문)와 `lines`(확정본)가 이미 저장되므로 **별도 로그 없이**
diff 로 정정 쌍을 뽑을 수 있다. SC-006 이 이를 전제한다.

먼저 할 일은 **오프라인으로 몇 건 뽑아보고 같은 오인식이 반복되는지 확인**하는 것.
반복이 확인되면 그때 기능으로 만든다(헌법 VII). 토큰 단위 diff 가 필요하다 —
항목 추가·삭제·병합·분할이 일어나면 줄 단위 대응이 깨진다.

### ~~N14. 쉼표 없는 표기~~ — 2026-09-14 판단: 코드 변경 없음

두 번째 사진에 쉼표가 한 개도 없었다(`65%(3+1)X2 70%(3+1)X2`). 현재 배치 규칙은
괄호 밖 쉼표로만 나누므로(`display-layout.ts`) 이런 줄은 한 줄로 그려진다.

`%` 단위 분할을 검토했으나 **반례가 있다** — 판5 `Power Snatch 75% 2x3, Snatch 80% 2x2`
를 강도 시작 위치로 자르면 `["Power Snatch", "75% 2x3, Snatch", "80% 2x2"]` 가 되어
두 번째 종목명이 앞 조각 꼬리에 붙는다. 저장본 4건 중 3건이 쉼표 표기라 쉼표 규칙을
버릴 수도 없다.

**결론: 사용자가 확인 단계에서 쉼표를 넣어 정규화한다. 코드는 안 고친다.**

- 이미 동작한다. 편집 버퍼가 쉼표↔줄바꿈이라 **Enter 도 되고 쉼표 직접 타이핑도 된다**
  (`ProgramItemRow.tsx`, `fromEditBuffer` 는 줄바꿈만 치환하므로 친 쉼표는 그대로 남는다)
- 칠판이 원래 쉼표를 안 쓰는지, OCR 이 날린 건지 **아직 모른다**. 배치 규칙을 바꾸면
  그 미확정 위에 코드를 얹는 셈이다
- 사용자가 쉼표를 넣는 행위 자체가 `source_text`↔`lines` diff 에 남아 N12 의 근거가 된다
- 모바일 비용은 쉼표 입력이 아니라 **캐럿 배치**다. N5 와 같은 병목이다

편집 부담이 실제로 못 견딜 수준으로 확인되면 그때 하이브리드(쉼표가 있으면 쉼표로,
없으면 강도 시작 위치로)를 꺼낸다. 지금은 근거가 없다(헌법 VII).

### N13. 드롭다운/구조화 입력 — 지금은 불가

문서 5장 미확정 항목이 막고 있었으나 2026-09-11 에 둘이 해소됐다
(렙×세트 순서 = 5렙 3세트, `+` 는 항상 괄호 안).

**남은 차단 요인**:
- 괄호 두 용법(복합 렙 vs modifier) 판정 기준 미확정 (3.9 / 5.2)
- 확인된 축약(`hsoc`, `FSQ`, `c.d.l`, `s.` 계열)이 `gym_exercises` 시드에 있는지 대조 안 됨 (7장)
- 이 브랜치에서 폼 입력 8개를 방금 지웠다(research R4). 되살리는 건 그 결정을 뒤집는 일이다

---

## 참고 — 이 브랜치에서 내린 결정

- `programs.lines` 가 `NULL` 이 아니면 텍스트 프로그램, `NULL` 이면 레거시. `parsed_data` 는 보지 않는다
- CHECK 제약에 `array_length` 를 쓰면 안 된다. 빈 배열에서 `NULL` 을 돌려줘 제약이 통과한다. `cardinality` 를 쓴다
- 러너 진입은 `LibraryItem.isRunnable`(레거시만 `true`)로 가른다
- `types_db.ts` 는 `programs` 블록만 수동 반영했다. 로컬 Supabase 가 다른 워크트리의 스키마를 함께 담고 있어 전체 재생성하면 이 브랜치에 없는 테이블이 섞인다
- FR-026 의 기준은 「문자를 바꾸거나 버리지 않는다」다. 배치·강조 변경은 FR-026a 로 허용된다
- **러너는 이 브랜치가 건드리지 않았다.** `git diff origin/main...HEAD -- features/program-runner`
  가 비어 있다. 지운 것은 폼 입력 경로(`ProgramForm.tsx` 등 8개 + `model/update.ts`)뿐이다.
  따라서 「레거시를 유지할까」는 미결이 아니다 — 과거 `parsed_data` 행은 계속 돌아간다.
  quickstart 6절은 새 기능 검증이 아니라 목록 화면(`isRunnable` 분기, 종목 필터) 회귀 확인이다
